# RaceDay Database Configuration

# Production Database (PostgreSQL) - Required for Vercel deployment
# Use one of these providers: Supabase, Neon, Railway, PlanetScale
DATABASE_URL="postgresql://username:password@hostname:port/database?schema=public"

# Environment
NODE_ENV="production"

# Frontend URL for CORS (update with your actual domain)
FRONTEND_URL="https://your-domain.vercel.app"

# === Database Provider Examples ===

# Supabase (Recommended)
# DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"
# VITE_SUPABASE_URL="https://[project-ref].supabase.co"
# VITE_SUPABASE_ANON_KEY="[your-anon-key]"

# Neon
# DATABASE_URL="postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/neondb"

# Railway  
# DATABASE_URL="postgresql://postgres:password@containers-us-west-1.railway.app:5432/railway"

# PlanetScale (MySQL - requires different Prisma provider)
# DATABASE_URL="mysql://username:password@hostname:port/database"

# === Development ===
# For local development, use SQLite (no setup required)
# The app will automatically use SQLite if DATABASE_URL is not set in development
